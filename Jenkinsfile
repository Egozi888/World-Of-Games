pipeline {
    agent any

    stages {
        stage('Initial_stage') {
            steps {
                sh '''
                    ls -la
                    // python3 click.py 
                    // python3 welcome.py
                    '''
            }
        }
    }
}